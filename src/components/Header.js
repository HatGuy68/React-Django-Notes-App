import React from 'react'

const Header = () => {
    return (
        <div>
            <h1>My Header</h1>
            {/**
              * Each component needs to be wrapped inside a container, i.e.
              * Shitfing this line below breaks the code.
              **/}
        </div>
    )
}

export default Header;